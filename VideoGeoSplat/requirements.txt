# Core dependencies (assumes VGGT and 3DGS are already installed in the Docker image)
numpy>=1.24
torch>=2.0
opencv-python>=4.8
pillow>=10.0
matplotlib>=3.7
trimesh>=3.21
plyfile>=1.0
pyyaml>=6.0
tqdm>=4.65

# Optional: for geometry export (mesh reconstruction)
open3d>=0.17

# Optional: for semantic labeling (Priority 3)
# groundedsam
# segment-anything
# groundingdino
